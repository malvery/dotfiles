general {
    lock_cmd          = pgrep -u ${USER} hyprlock || hyprlock -q
    before_sleep_cmd  = hyprlock -q

    inhibit_sleep     = 3
}

# listener {
#     timeout     = 600
#     on-timeout  = loginctl lock-session
# }

listener {
    timeout     = 300
    on-timeout  = ~/bin/wlr-dpms.sh --off
    on-resume   = ~/bin/wlr-dpms.sh --on
}
