# keyboard
input * {
	xkb_layout	"us,ru"
	xkb_options	"grp:caps_toggle"
}

# touchpad
input * {
	events enabled
	tap enabled
	natural_scroll enabled
	scroll_factor 0.3
	dwt enabled
}

# hotkeys
bindsym --to-code {
	Ctrl+grave exec clipman pick --tool="rofi"
}
