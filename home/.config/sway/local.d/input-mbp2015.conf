# keyboard
input * {
	xkb_layout "us,ru(mac)"
	xkb_options "grp:caps_toggle"
}

# touchpad
input * {
	events enabled
	tap disabled
	natural_scroll enabled
	dwt enabled
	pointer_accel 0.3
}

# hotkeys
bindsym --to-code {
	Ctrl+less exec clipman --selector="rofi" -s
}
